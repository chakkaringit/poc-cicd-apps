apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ebs-persist-pvc
  namespace: opolo
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.persistence.size | default "5Gi" }}
  storageClassName: efs-sc
